export const repoSearchURL = `https://api.github.com/search/repositories?q=`; //${searchParameter}
export const userSearchURL = `https://api.github.com/search/users?q=`; //${searchParameter}
export const repoURL = `https://api.github.com/repos/`; //${repoTitle}
export const userURL = `https://api.github.com/users/`; //${userName}
export const userReposURL = `https://api.github.com/users/`; //`${userName} /repos`
